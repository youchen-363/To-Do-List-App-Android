<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TodoList" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:itemBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:actionMenuTextColor">@color/app</item>
    </style>

    <style name="Base.Theme.TodoListNoMenu" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/app</item>
        <item name="colorAccent">@color/app</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:itemBackground">@color/white</item>
    </style>

    <style name="Theme.TodoList" parent="Base.Theme.TodoList" />
</resources>